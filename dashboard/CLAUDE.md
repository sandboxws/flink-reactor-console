# Dashboard Conventions

## Styling

- Dropdowns (`SelectContent`) and popover panels (`HoverCardContent`, `PopoverContent`) must use `bg-dash-panel border-dash-border` — not `bg-dash-elevated`. This keeps all floating surfaces visually consistent with the DAG operator node hover cards.
