# reactor-server

Go backend providing a GraphQL API that proxies Flink REST, manages WebSocket subscriptions, and watches Kubernetes CRDs for blue-green deployment monitoring.

## Architecture

- **HTTP framework**: Echo v4 (labstack/echo)
- **GraphQL**: gqlgen (99designs/gqlgen) with schema-first approach
- **WebSockets**: gorilla/websocket (via gqlgen transport)
- **Kubernetes**: client-go dynamic client for CRD watching
- **Observability**: Prometheus client_golang + structured slog logging

Request flow:
```
Dashboard → Echo (CORS, logging, recovery) → /graphql → gqlgen resolvers
                                                         ├─ Flink REST proxy
                                                         ├─ K8s CRD watcher
                                                         ├─ SQL Gateway proxy
                                                         └─ Instrument queries
```

## Package Layout

Module path: `github.com/sandboxws/flink-reactor-console/server`. There is no
`go.work` — `server/` builds standalone.

```
server/
├── cmd/
│   ├── reactor-server/     # Binary entrypoint
│   │   └── main.go         # Signal handling, config parsing, server lifecycle
│   └── migrate/            # goose migration CLI (up/down/status/reset/version)
├── internal/
│   ├── alerts/             # Alert engine, evaluator, Postgres LISTEN/NOTIFY bridge
│   ├── bgsync/             # Background sync of Flink data → Postgres (per-domain tickers)
│   ├── catalogs/           # Catalog providers (bundled YAML, SQL Gateway)
│   ├── cluster/            # Multi-cluster config, connection manager, health polling
│   ├── compatibility/      # State-manifest compatibility rules + verdicts
│   ├── config/             # Shared configuration types
│   ├── connector/          # Source/sink connector detection
│   ├── flink/              # Flink REST API client, response mappers, EventBus
│   ├── graphql/            # gqlgen resolvers + generated code
│   │   ├── schema/         # 24 .graphqls schema files (source of truth)
│   │   └── generated/      # Auto-generated by gqlgen (do not edit)
│   ├── instruments/        # Pluggable infrastructure instruments (registry, health)
│   │   ├── database/       # PostgreSQL + MySQL (schema browse, query, highlight)
│   │   ├── datalake/       # Iceberg REST + Paimon filesystem catalogs
│   │   ├── fluss/          # Fluss databases, tables, TabletServer health
│   │   ├── kafka/          # Topics, consumer groups
│   │   ├── redis/          # Keys, server info, memory stats
│   │   └── schemaregistry/ # Subjects, versions, compatibility checks
│   ├── integration/        # Integration test helpers
│   ├── k8s/                # Kubernetes dynamic client, BG deployment watcher
│   ├── logs/               # Log proxy endpoints (plain text, not GraphQL)
│   ├── manifests/          # Pipeline manifest ingest
│   ├── materialized/       # Materialized table service
│   ├── metricsproxy/       # Flink metrics JSON proxy (REST, not GraphQL)
│   ├── observability/      # Prometheus metrics, slog logger, middleware
│   ├── savepoints/         # Savepoint trigger-type cache
│   ├── server/             # Echo server setup, middleware chain, health endpoints
│   ├── simulation/         # Chaos-simulation engine (scenarios are stubs)
│   ├── spa/                # SPA static file serving (dashboard)
│   ├── sqlgateway/         # Flink SQL Gateway REST client
│   ├── storage/            # Postgres pool + embedded goose migrations
│   ├── store/              # Per-domain Postgres stores
│   └── tap/                # Tap manifest loader
├── justfile                # Task runner (18 recipes: dev/build/test/lint/generate/fmt/docker/db-*/ci)
├── Dockerfile              # Multi-stage build, non-root user, port 8080
├── go.mod / go.sum
└── gqlgen.yml              # GraphQL code generation config
```

> `internal/bgsync` and `internal/metricsproxy` are named to avoid shadowing
> the stdlib `sync` and `runtime/metrics` packages.

## Development Commands

All commands run from `server/`:

| Command | What It Does |
|---------|-------------|
| `just dev` | Run server with `go run` (hot-reload via manual restart) |
| `just build` | Build binary to `bin/reactor-server` |
| `just test` | Run all tests |
| `just lint` | Run golangci-lint |
| `just generate` | Run gqlgen codegen (`go generate ./...`) |
| `just fmt` | Format with gofumpt |
| `just docker` | Build Docker image tagged `reactor-server` |
| `just coverage` | Run tests with coverage, generate HTML report |
| `just ci` | Full pipeline: build → lint → test |

## GraphQL Schema

Schema-first approach: 24 `.graphqls` files in `internal/graphql/schema/` define the API. After editing schemas, run `just generate` to regenerate resolvers.

**Schema files by domain:**
- `schema.graphqls` — Root types, `ClusterInfo`, `health`, `clusters` queries
- `jobs.graphqls` — Job overview, detail, vertices, checkpoints, flamegraphs
- `bluegreen.graphqls` — Blue-green deployment types, queries, subscription
- `subscription.graphqls` — `jobStatusChanged`, `sqlResults` subscriptions
- `config.graphqls` — `flinkConfig`, `dashboardConfig` queries
- `jars.graphqls` — JAR management queries and mutations
- `sqlgateway.graphqls` — SQL Gateway session/statement mutations
- `kafka.graphqls` — Kafka topic and consumer group queries
- `instruments.graphqls` — Instrument registry query + connection-test mutation
- `database.graphqls` — Database schema browsing, table detail, query execution, query history
- `taskmanagers.graphqls` — Task manager overview, detail, logs, thread dump
- `jobmanager.graphqls` — Job manager config, environment, metrics, HA status
- `tap.graphqls` — Tap manifest query
- `applications.graphqls` — Flink 2.3 application mode (FLIP-549)
- `alerts.graphqls` — Alert rules, active alerts, history, fired/resolved subscriptions
- `compatibility.graphqls` — Pipeline manifests, state-compatibility verdicts, restore events
- `history.graphqls` — Postgres-backed job/checkpoint/exception/metric history
- `catalogs.graphqls` — Catalog, database, table, column browsing + DDL
- `materialized.graphqls` — Materialized tables (suspend/resume/refresh)
- `simulation.graphqls` — Chaos-simulation preflight, runs, presets
- `sources_sinks.graphqls` — Connector detection on `JobDetail`
- `redis.graphqls` — Redis scan, key info/value, server info, memory stats
- `schemaregistry.graphqls` — Subjects, versions, schema detail, compatibility check, global default config
- `fluss.graphqls` — Fluss databases, tables, tablet servers

## Key Patterns

### Cluster Scoping
Most GraphQL queries accept an optional `cluster: String` argument. The resolver uses `Manager.Resolve(cluster)` to find the correct `Connection`, which routes to the right Flink REST URL. If omitted, the default cluster is used.

### Kubernetes Dynamic Client
The K8s integration uses `client-go/dynamic` (not typed clients) because `FlinkBlueGreenDeployment` is a CRD — no generated Go types exist. The `k8s.Client` wraps `dynamic.Interface` and manually parses unstructured JSON into domain types (`k8s.BlueGreenDeployment`).

### Instrument Interface

Instruments are pluggable connectors to the infrastructure around a job:
`database`, `datalake`, `fluss`, `kafka`, `redis`, `schemaregistry`. Each
implements the 9-method `Instrument` interface (`Name`, `DisplayName`, `Version`,
`Type`, `Init`, `Shutdown`, `HealthCheck`, `Capabilities`, `HighlightResources`)
and is registered in `instruments.Registry` from `main.go`. Health checks run on
the same interval as cluster health checks. Instruments are optional — the server
starts fine without any. None are configured by default **except in
development**, where a default set targeting the local DSL docker-compose stack
(Kafka, Postgres, Schema Registry, Fluss, Iceberg) is synthesized when the YAML
declares none (`defaultDevelopmentInstruments`, `internal/config/config.go`).
Any explicit `instruments:` list suppresses the defaults.

**Environment tag + Kafka seeding.** Each instrument config may carry an
`environment` tag (`development` / `staging` / `production`); when omitted it is
inferred from the instrument name and the server env (`InferInstrumentEnv`). It
gates environment-sensitive operations — today, Kafka **seeding** (producing the
DSL's sample fixtures into topics). Seeding is enabled by default in development,
opt-in in staging via `seeding.enabled`, and hard-blocked in production on both
the server env AND the instrument env (`internal/config/seeding.go`,
`SeedingAllowed`). The Kafka instrument advertises a `seed` capability only when
the policy permits, and the `seedKafkaTopics` mutation re-checks it server-side.
By default that mutation seeds only this project's topics (catalog subjects
whose topic already exists in the broker); `allTopics: true` seeds the entire
sample catalog, creating each topic. The seed catalog is embedded from the DSL
via `scripts/refresh-seeds.sh` (`internal/instruments/kafka/seed/`).

Seeding is now primarily a **`cluster up` responsibility** in the DSL — it
produces each declared source topic's sample rows automatically (idempotently),
so topics show data in the console without any manual step. The console's
`seedKafkaTopics` mutation is a manual re-seed / top-up on top of that. The
`kafkaTopicMessages(instrument, topic, limit)` query previews the most recent
records via a throwaway direct-partition consumer (`Client.PreviewMessages`,
no consumer group is left behind).

`HealthReporter` is injected into the registry via a functional option; the
console supplies a Prometheus adapter (`observability.InstrumentHealthAdapter`).
That adapter satisfies the interface **structurally, without importing the
instruments package**, so a grep for the import path will not find it.

**Adding a new instrument:**

1. Create `internal/instruments/<name>/` implementing the `Instrument` interface.
2. Add a registration case in `cmd/reactor-server/main.go`.
3. Add `internal/graphql/schema/<name>.graphqls` and run `just generate`.
4. Add a `resolve<Name>Instrument` helper in `internal/graphql/helpers.go`.
5. Dashboard: components under `dashboard/src/components/instruments/<name>/`,
   routes under `dashboard/src/routes/instruments/$instrumentName/<name>/`, and
   the icon map in `components/instruments/instrument-icons.tsx`.

> `datalake` is registered in `main.go` but has no GraphQL resolver, no
> `InstrumentType` union member, and no dashboard UI — it is Go-side only.

### EventBus
`flink.EventBus[T]` is a generic pub/sub bus used for WebSocket subscriptions. Watchers (job status poller, K8s CRD watcher) publish events; GraphQL subscription resolvers subscribe. Subscribers receive a channel and must read from it to avoid blocking.

## Configuration

All non-secret configuration is defined in YAML config files under `config/`. The environment-specific file is selected by `REACTOR_ENV` or `APP_ENV` (defaults to `development`).

### Environment Variables (secrets only)

These are the only environment variables the server reads. Everything else must
come from YAML (`bindSecretEnvVars`, `internal/config/config.go`).

| Variable | Description |
|----------|-------------|
| `REACTOR_ENV` / `APP_ENV` | Selects which YAML config file to load (e.g. `production` → `config/production.yml`) |
| `FLINK_AUTH_TOKEN` | Bearer token for Flink API auth |
| `FLINK_INIT_SQL_PATH` | Path to SQL run against the SQL Gateway on session init |
| `REACTOR_STORAGE_DSN` | Postgres DSN, overriding `storage.dsn` |

> Consequences worth knowing: `STATIC_DIR` (set by the Dockerfile) and
> `FLINK_REST_URL` / `FLINK_SQL_GATEWAY_URL` (passed by `just docker-run`) are
> **not** bound, and the runtime image ships no `config/` directory. The
> container therefore runs on defaults and does not serve the dashboard it
> carries.

### YAML Config Structure

```yaml
app:
  debug: true

server:
  host: "0.0.0.0"
  port: 8080
  read_timeout: "30s"
  write_timeout: "30s"
  drain_timeout: "30s"

flink:
  rest_url: "http://localhost:8081"
  sql_gateway_url: "http://localhost:8083"
  # auth_token: set via FLINK_AUTH_TOKEN env var

# Multi-cluster mode (overrides flink.* for cluster creation):
# clusters:
#   - name: "production"
#     url: "http://flink-prod:8081"
#     sql_gateway_url: "http://sqlgw-prod:8083"
#     default: true
#   - name: "staging"
#     url: "http://flink-staging:8081"

health:
  interval: "30s"

log:
  level: "info"       # debug, info, warn, error
  format: "console"   # console, json

spa:
  static_dir: ""

seeding:
  enabled: false   # opt-in for staging; ignored in dev (always on) and prod (always off)

instruments:
  - type: "kafka"
    name: "local-kafka"
    environment: "development"   # gates seeding; inferred from name/server env when omitted
    config:
      brokers: "localhost:9092"
```

## Cross-References

- Root CLAUDE.md "Go Conventions" section — formatter, import groups, linters
- `internal/graphql/schema/*.graphqls` — GraphQL API source of truth
- `Dockerfile` — Multi-stage build: golang:1.26-alpine → alpine:3.21, non-root user, port 8080
