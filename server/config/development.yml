app:
  debug: true

server:
  host: "0.0.0.0"
  port: 8080

flink:
  rest_url: "http://localhost:8081"
  sql_gateway_url: "http://localhost:8083"

health:
  interval: "30s"

log:
  level: "info"
  format: "console"
