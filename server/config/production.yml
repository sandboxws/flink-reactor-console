app:
  debug: false

server:
  host: "0.0.0.0"
  port: 8080
  read_timeout: "60s"
  write_timeout: "60s"
  drain_timeout: "30s"

# flink.rest_url, flink.auth_token, sql_gateway.url: set via env vars

health:
  interval: "30s"

log:
  level: "warn"
  format: "json"
